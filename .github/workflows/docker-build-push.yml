name: Build and Push LMS Frontend & Backend Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Step 1️⃣ - Checkout code
      - name: 🛎️ Checkout Repository
        uses: actions/checkout@v4

      # Step 2️⃣ - Login to Docker Hub
      - name: 🔑 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Step 3️⃣ - Build & Push Frontend (React)
      - name: 🏗️ Build and Push Frontend Image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend/lms-react
          file: ./frontend/lms-react/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/lms-frontend:latest

      # Step 4️⃣ - Build & Push Backend (Spring Boot)
      - name: 🏗️ Build and Push Backend Image
        uses: docker/build-push-action@v4
        with:
          context: ./backend/lms-springboot
          file: ./backend/lms-springboot/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/lms-backend:latest
